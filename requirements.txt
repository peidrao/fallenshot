# Python deps para fallenshot
# Instale via: pip install -r requirements.txt
# OU use apt (preferido para GTK apps):
#   sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-4.0 python3-dbus gir1.2-ayatanaappindicator3-0.1

# PyPI
PyGObject>=3.44.0
dbus-python>=1.3.2
pycairo>=1.23.0
    ´´